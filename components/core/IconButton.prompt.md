Circular icon-only button for toolbars and nav rails.

```jsx
<IconButton onDark><BellIcon/></IconButton>
```

Set `onDark` on navy backgrounds. `active` shows a persistent tinted fill (e.g. selected nav item).
