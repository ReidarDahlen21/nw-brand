Elevated rounded container — 16px radius, soft shadow, thin hairline border.

```jsx
<Card><h3>Patrimonio total</h3></Card>
```

Set `onDark` when placing on navy backgrounds (uses a lighter navy tint + darker shadow instead of the light shadow).
