Thin wrapper around a Phosphor Icons (`regular` weight) glyph — the system's icon set. Requires the Phosphor stylesheet loaded once per page (see `guidelines/iconography.html` or `templates/*/ds-base.js`).

```jsx
<Icon name="house" size={18} />
```

Browse names at phosphoricons.com — pass the bare name (no `ph-` prefix).
